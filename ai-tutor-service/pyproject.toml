[project]
name = "ai-tutor-service"
version = "0.1.0"
description = "AI Tutor Service - 口頭試問AI秘書 (独立HTTP API)"
requires-python = ">=3.12"

[tool.ruff]
line-length = 100
target-version = "py312"

[tool.pytest.ini_options]
pythonpath = ["."]
testpaths = ["tests"]
