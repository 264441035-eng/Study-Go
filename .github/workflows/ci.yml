name: ci

on:
  pull_request:
  workflow_dispatch:

jobs:
  backend:
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: backend
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: "3.12"
      - run: pip install -r requirements-dev.txt
      - name: Lint (ruff)
        run: ruff check .
      - name: Test (pytest)
        run: pytest -q

  # ユニットテスト(in-memory)では通ってしまう「起動時クラッシュ」を merge 前に検出する。
  # 本番と同じ Dockerfile でイメージをビルドし、隣に Postgres を立てて
  #   1) デプロイと同じスキーマ初期化 `python -m app.dbctl init-db` が実 DB に対して通るか
  #   2) uvicorn が起動して /health・/api/hello が 200 を返すか
  # を検査する（PR #7 で発生した DB 接続起因のクラッシュループの再発防止）。
  backend-smoke:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Build backend image
        run: docker compose build backend

      - name: Start Postgres
        run: docker compose up -d db

      - name: Schema init (production migration path)
        run: docker compose run --rm backend python -m app.dbctl init-db

      - name: Start backend & wait for /health
        run: |
          docker compose up -d backend
          for i in $(seq 1 30); do
            if curl -fsS http://localhost:8000/health; then echo; echo "backend healthy"; exit 0; fi
            sleep 2
          done
          echo "backend did not become healthy in time"; exit 1

      - name: Smoke test /api/hello
        run: curl -fsS http://localhost:8000/api/hello

      - name: Show container logs on failure
        if: failure()
        run: docker compose logs

      - name: Teardown
        if: always()
        run: docker compose down -v

  frontend:
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: frontend
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: "22"
          cache: npm
          cache-dependency-path: frontend/package-lock.json
      - run: npm ci
      - name: Lint (eslint)
        run: npm run lint
      - name: Build (type-check + vite build)
        run: npm run build
      - name: Test (vitest)
        run: npm test
